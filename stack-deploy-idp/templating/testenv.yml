---
apiVersion: v1
kind: Namespace
metadata:
  name: ($ .org $)-($ .component $)
---
# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ($ .org $)-($ .component $)
  namespace: ($ .org $)-($ .component $)
spec:
  replicas: ($ .app_replicas $)
  selector:
    matchLabels:
      app: ($ .org $)-($ .component $)
  template:
    metadata:
      labels:
        app: ($ .org $)-($ .component $)
    spec:
      containers:
        - name: ($ .org $)-($ .component $)
          image: ($ .dockerhub_repository $):($ .org $)-($ .project $)-($ .app_image_tag $)
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: ($ .org $)-($ .component $)
  namespace: ($ .org $)-($ .component $)
spec:
  selector:
    app: ($ .org $)-($ .component $)
  ports:
    - port: 80
      targetPort: 8080
---
# Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ($ .org $)-($ .component $)
  namespace: ($ .org $)-($ .component $)
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  rules:
    - host: ($ .component $).($ .org $).($ .dns_zone $)
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ($ .org $)-($ .component $)
                port:
                  number: 80
  tls:
    - hosts:
        - ($ .component $).($ .org $).($ .dns_zone $)
      secretName: ($ .org $)-($ .component $)-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ($ .org $)-($ .component $)
  namespace: ($ .org $)-($ .component $)
spec:
  secretName: ($ .org $)-($ .component $)-tls
  issuerRef:
    name: ssl-issuer
    kind: ClusterIssuer
  dnsNames:
    - ($ .component $).($ .org $).($ .dns_zone $)